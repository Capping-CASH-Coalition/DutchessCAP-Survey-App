import { ExceptQuestionIdPipe } from './exceptQuestionId.pipe';

describe('ExceptQuestionIdPipe', () => {
  it('create an instance', () => {
    const pipe = new ExceptQuestionIdPipe();
    expect(pipe).toBeTruthy();
  });
});
