import { GraphableQuestionPipe } from './graphableQuestion.pipe';

describe('GraphableQuestionPipe', () => {
  it('create an instance', () => {
    const pipe = new GraphableQuestionPipe();
    expect(pipe).toBeTruthy();
  });
});
